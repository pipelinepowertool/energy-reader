name: CICD

on:
  push:
    branches: [ "master" ]
    
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: make
        run: make
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: ls    
        
